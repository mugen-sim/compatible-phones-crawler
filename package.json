{
  "name": "mugen-sim",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Crawler that publishes smartphone eSIM support data as static JSON.",
  "scripts": {
    "crawl:devices": "node scripts/crawl-devices.mjs"
  },
  "dependencies": {
    "playwright": "^1.56.1"
  },
  "engines": {
    "node": ">=24"
  }
}
